import { createSlice } from "@reduxjs/toolkit";


const initialState = []

const commentSlice = createSlice({
    name: 'commentSlice',
    initialState,
    reducers: {

    }

})

export default commentSlice;