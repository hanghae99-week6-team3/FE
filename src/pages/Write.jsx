import React from 'react';
import React from "react";
import Layout from "../components/common/Layout";

const Write = () => {
    return (
        <div>

        </div>
    );
  return <Layout></Layout>;
};

export default Write;export default Write;export default Write;export default Write;
